import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    errorTextStyle: {
        fontSize: 20,
        alignSelf: 'center',
        color: 'red'
    },
});

export const { errorTextStyle } = style;
